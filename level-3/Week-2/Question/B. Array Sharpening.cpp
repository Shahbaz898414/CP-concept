#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
  ll t;
  cin >> t;

  while (t--)
  {
    ll n;
    cin >> n;

    vector<int> a(n);

    ll l = 0, p = 0, r = 0, ans = 0;

    for (auto &i : a)
      cin >> i;
    
    ll sum = 0;
    
  }
}