#include <bits/stdc++.h>

#define ll long long
using namespace std;


class Solution {
public:
    vector<int> nextGreaterElements(vector<int>& nums) {
        
    }
};

int main() {
  int n;cin>>n;

  vector<int>  v(n);

  for (int i = 0; i < n; i++ ) {

    cin >>arr[i];
  }

  vector<int>  num=nextGreaterElements(v);

  for(auto it:num){
    cout<<it<<" ";
  }
  
  cout<<endl;
}
