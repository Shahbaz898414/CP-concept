#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
  ll n, m;
  cin >> n >> m;

  ll arr[n + 1][m + 1];

  for (ll i = 1; i <= n; i++)
  {

    for (ll j = 1; j <= m; j++)
    {

      cin >> arr[i][j];
    }
  }

  ll ans[n + 1][m + 1];

  for (int i = 1; i <= n; i++)
  {
    /*  */
    for (int j = 1; j <= m; j++)
    {
      /*  */
    }
  }

  while (m-- > 0)
  {
  }
}
